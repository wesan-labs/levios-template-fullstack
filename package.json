{
  "name": "my-levios-store",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "dev:backend": "turbo run dev --filter=backend",
    "dev:storefront": "turbo run dev --filter=storefront",
    "seed": "turbo run seed --filter=backend"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
